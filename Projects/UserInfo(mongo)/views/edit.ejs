<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-gray-900 to-black text-white min-h-screen flex flex-col items-center justify-center p-6">

    <div class="w-full max-w-md bg-gray-800 p-8 rounded-2xl shadow-2xl transform transition-all hover:scale-105">
        <form class="flex flex-col items-center" action="/update/<%= user._id %>" method="post">

            <h1 class="py-7 text-5xl font-extrabold text-center text-white bg-clip-text bg-gradient-to-r from-cyan-400 via-blue-500 to-indigo-600 drop-shadow-2xl uppercase tracking-wide">
                Edit User
            </h1>
            <div class="w-48  h-48 my-4 flex justify-center items-center overflow-hidden bg-gray-600 rounded-full">
                <img class="w-full h-full object-cover" src="<%= user.image %>" alt="user">
            </div>
            
            <input class="w-full my-2 px-5 py-3 mb-4 bg-gray-700 text-green-300 rounded-lg outline-none focus:ring-2 focus:ring-green-400 placeholder-gray-400 transition duration-300" 
                   type="text" placeholder="Name" name="name" value="<%= user.name %>" required>
            <input class="w-full my-2 px-5 py-3 mb-4 bg-gray-700 text-green-300 rounded-lg outline-none focus:ring-2 focus:ring-green-400 placeholder-gray-400 transition duration-300" 
                   type="email" placeholder="Email" name="email" value="<%= user.email %>" required>
            <input class="w-full my-2 px-5 py-3 mb-4 bg-gray-700 text-green-300 rounded-lg outline-none focus:ring-2 focus:ring-green-400 placeholder-gray-400 transition duration-300" 
                   type="text" placeholder="Image Url" name="image" value="<%= user.image %>">               
            <button class="w-full my-4 px-5 py-3 bg-gradient-to-r from-yellow-500 to-orange-500 hover:from-orange-500 hover:to-yellow-500 text-white font-bold rounded-lg shadow-md hover:shadow-lg transition-all duration-300">
                Update User
            </button>
        </form>
    </div>

    <div class="max-w-6xl flex justify-end p-4">
        <a href="/read" class="text-blue-400 m-4 hover:text-blue-500 text-lg font-semibold transition duration-300">CANCEL EDIT</a>
    </div>

</body>
</html>
